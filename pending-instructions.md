Plan the score rendering scheme. It will be SVG-based for most elements and plain HTML for layout. A mix of the
two is permissable for grosser elements. All components should be represented in storybook.

This will likely be a lot of code, so just propose a strategy first, along with a checklist of the components
you think we will need.

I am not concerned with editing just yet; I think it will fall into place naturally if our rendering system
is properly factored.

Also call out major assumptions and potential problems.
